Include config.d/*

# Bitbucket doesn't support multiplexing
Host bitbucket.org
    ControlMaster no

Host *
    ServerAliveInterval 60
    Compression yes
    ForwardAgent no

    # Multiplexing
    ControlMaster auto
    ControlPath /tmp/ssh_%C
    ControlPersist 10m
